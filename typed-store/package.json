{
  "name": "typed-store",
  "version": "0.1.0",
  "description": "Minimal typed reducer store for app state",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "index.ts",
    "browser.ts"
  ],
  "scripts": {
    "build": "pnpm run build:module && pnpm run build:browser",
    "build:module": "tsgo -p tsconfig.json",
    "build:browser": "esbuild browser.ts --bundle --platform=browser --format=iife --global-name=TypedStore --outfile=dist/browser.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "keywords": [
    "state",
    "store",
    "reducer",
    "typescript",
    "frontend"
  ],
  "license": "MIT"
}
