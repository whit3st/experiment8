{
  "name": "typed-bus",
  "version": "0.1.0",
  "description": "Deterministic typed pub/sub bus for hexagonal architecture",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "index.ts",
    "browser.ts"
  ],
  "scripts": {
    "build": "pnpm run build:module && pnpm run build:browser",
    "build:module": "tsgo -p tsconfig.json",
    "build:browser": "esbuild browser.ts --bundle --platform=browser --format=iife --global-name=TypedBus --outfile=dist/browser.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "keywords": [
    "pubsub",
    "event-bus",
    "typescript",
    "hexagonal-architecture",
    "ports-and-adapters"
  ],
  "license": "MIT"
}
